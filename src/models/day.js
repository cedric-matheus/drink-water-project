/**
 * @typedef Day
 * @type {object}
 * @property {string} date - Data correspondente ao dia.
 * @property {number[]} totalObjectiveWater - Total de água do dia (0=min e 1=max).
 * @property {number} totalWaterDrunk - Total de água ingerida.
 * @property {number[]} waterDrunkDifference - Diferença entre água ingerida e objetivo (0=min e 1=max).
 */

export {};